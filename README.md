# SpringSecurity
Secure REST API built using Spring Boot and Spring Security with JWT-based authentication and role-based authorization. Implements stateless authentication, custom filters, and database-backed user management.
